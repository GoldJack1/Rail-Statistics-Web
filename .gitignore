.DS_Store
node_modules/

# Local env and credentials — never commit
.env
.env.*
!.env.example
!.env*.example
*.pem
credentials.json
**/.vitest-secret-token

# Leftover path after rename New Site → new-site (local .next cache only)
/New Site/

# Reference-only copy of the old Vite/React site — tracked separately in its
# own repo (https://github.com/GoldJack1/railstatisticsweb.git), never in this one.
/Old Website\ /

# Local credentials — never commit
/ignore/
